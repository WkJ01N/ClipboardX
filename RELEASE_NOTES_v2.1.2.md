# ClipboardX v2.1.2 - 自动粘贴、卡片动画与安装体验修复

### 本次维护版本集中解决 v2.0/v2.1 遗留的卡片重排动效问题，并修复 v2.1 中辅助功能权限误判导致的自动粘贴回归。

## 🛠 改进与修复 (Improvements & Bug Fixes)

- **自动粘贴恢复**：辅助功能权限改用 macOS Accessibility 信任状态判断，不再因额外预检误报“需要辅助功能权限”。
- **目标应用恢复**：每次呼出面板都会重新记录目标应用，粘贴前等待其恢复前台，避免粘贴到 ClipboardX 或过期目标。
- **浮动飞升动效**：移除会产生错误运动路径的匹配几何动画，靠下卡片现在会稳定向所属分组顶部移动。
- **闪现淡入动效**：采用“渐隐—重排—渐显”三阶段过渡，其他卡片会在重排时同步下移，动画时长与设置保持一致。
- **损坏内容保护**：图片负载缺失、敏感文本无法解密或文件全部丢失时不再发送空粘贴；多文件部分缺失时可选择只粘贴仍存在的文件。
- **持久化反馈**：置顶、固定、收藏、删除和清理操作失败时会回滚并显示错误，不再静默吞掉保存失败。

## 📦 安装方式 (Installation)

- 推荐下载 `ClipboardX.pkg`，应用将被安装到 `/Applications/ClipboardX.app`。
- 同时提供 `ClipboardX.zip` 作为便携备用包，解压后需手动拖入“应用程序”。

## ✅ 验证 (Verification)

- 单元测试与 UI 启动测试通过。
- Debug 与 universal Release 构建通过，支持 Apple Silicon 与 Intel Mac。
- PKG 负载、应用签名、Bundle ID、版本号和双架构均经过自动校验。

## 📦 版本信息 (Release Information)

- 应用版本：**2.1.2**
- 构建版本：**212**
- 最低系统版本：macOS 15

> 应用和安装包尚未经过 Apple 公证。首次运行时请右键选择“打开”，或在“系统设置 → 隐私与安全性”中点击“仍要打开”；无需全局关闭 Gatekeeper。
